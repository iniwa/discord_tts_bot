# プログラム改善チェックリスト

コードベースを調査して洗い出した改善候補の一覧。

**運用方法**: 着手したい項目にチェック `[x]` を入れる → Codex が handoff
（`docs/handoffs/`）を作成し、Claude Code（auto モード）が実装する。
handoff を挟むまでもない小粒な項目は Claude Code に直接依頼してもよい。
実装完了した項目は「完了アーカイブ」へ移動する。

- 機能追加・未検証項目はこのファイルの対象外（`docs/issues.md` 等で管理）。
- 優先度: **高** = 稼働中の安定性に直結 / **中** = 保守性・性能 / **低** = 任意。

---

## 1. 安定性

（現在なし）

## 2. 保守性

（現在なし）

## 3. 品質

- [ ] **【低】純関数の最小ユニットテストを追加する**
  - 現状: テストファイル0件（カバレッジ 0%）。`apply_dict()` / `strip_symbols()` は
    純関数で、辞書の長い単語優先・`w+` の英数字境界・括弧除去など
    リグレッションしやすい仕様を持つ。
  - 対応案: `tests/test_text.py` に pytest で10ケース程度。
  - 制約: requirements.txt / Dockerfile には pytest を追加しない
    （ローカル install のみ）。

---

## 完了アーカイブ

### 2026-07-07

- [x] **【高】Bot 自身が VC から強制切断された場合のクリーンアップ漏れを修正する**
  - Bot 自身の切断イベントでギルド状態と読み上げキューを破棄する処理を追加。
- [x] **【中】JSON 破損時に起動クラッシュしないようにする**
  - `word_dict.json` / `settings.json` の `JSONDecodeError` を警告ログ + 空 dict で継続。
- [x] **【中】open_jtalk / ffmpeg サブプロセスにタイムアウトを設定する**
  - Open JTalk 起動時ウォームアップ、音声生成、FFmpeg ウォームアップに10秒タイムアウトを追加。
- [x] **【中】読み上げキューに上限を設ける**
  - ギルドごとのキュー上限を20件にし、超過時は古い一時ファイルを削除して破棄。
- [x] **【中】on_message 内の辞書適用処理を `apply_dict()` に統合する**
  - 絵文字置換、括弧除去、ひらがな化の順序を保ったまま辞書置換を共通化。
- [x] **【低】未使用のプレフィックスコマンド機構を削除する**
  - 未使用の `bot.process_commands(message)` 呼び出しを削除。
- [x] **【低】docker-compose.yaml のコメント文字化けを修正する**
  - 辞書ファイル永続化コメントの置換文字を修正。
- [x] **【低】設定・辞書の保存をイベントループ外で行う**
  - 辞書・設定保存を `run_in_executor` 経由に変更。