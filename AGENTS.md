# AGENTS.md

This entry governs the Discord TTS bot.

## Facts and protected behavior

The bot integrates Discord and speech services. Keep real Discord tokens and credentials out of tracked files and reports; `word_dict.json` is sample data and changes only when explicitly targeted. Tests must isolate Discord and other external services. Preserve command and event behavior, data formats, and deployment configuration unless the task explicitly changes them.

Runtime, tool, organization, and safety rules precede explicit user instructions, this entry, and task scope. Facts do not grant authority. Preserve unrelated changes, history, and operator-managed artifacts; a dirty tree is not itself a blocker, but stop only the overlapping portion when intent cannot be resolved. Do not inspect or edit secrets, credentials, local settings, runtime or production state, generated heavy artifacts, dependencies, CI/CD, deployment, publication, or external exposure outside authorized scope, and never reproduce private values.

## Work and delegation

Within authorized scope, complete investigation, implementation, useful checks, corrections, and authorized application without making plans, first edits, or progress reports approval gates. Isolate blocked dependencies and continue independent safe work. Before a required approval, prepare the permitted candidate and checks that do not depend on it; do not assume unresolved choices. Current explicit user instructions override user-managed Skill defaults, while mandatory approvals and tool or organization limits remain effective. Explain a stop only when needed, citing a read file and applicable passage; distinguish requirement from interpretation and environment limits, never infer unread instructions, and do not create a routine stop log.

The primary owns design, implementation, integration, acceptance, and communication; the user's selected runtime model and effort remain unchanged. Child model/effort belongs to shared configuration: use an observable configured role, or continue through the primary/equivalent and report an unobserved or unused role honestly. Delegate only a separable useful outcome or a required/material-risk independent review. `bounded_implementer` implements, `bounded_explorer` and `bounded_reviewer` are read-only, and `adaptive_implementer` is a compatibility implementation name. Keep one writer for overlap, do not redelegate, and reclaim work only after the child has stopped editing and returned ownership. Preserve any concrete mandatory independent-review or pre-application gate: review a stable candidate, reaccept affected work after later changes, then apply and smoke only when authorized. Prefer existing CLI, API, MCP, and file operations; retain necessary GUI or visual acceptance. Use a persisted handoff only for interruption-sensitive, cross-session, risky, or separately executed work.

## Verification and records

Use the focused project test command documented by the repository when code behavior is in scope. Documentation-only work uses focused reference/fence scans and `git diff --check`; do not run application suites merely for coverage. Recheck only after a changed candidate, failure, or concrete unresolved risk. Report source readiness separately from unavailable operation checks and never report an unrun check as successful.

Custom token/efficiency collection, comparisons, and pending-harvest duties are retired. Preserve existing data and standard logs, notification, and recovery. The primary saves concise non-secret knowledge with scope, date, evidence, and confirmed/user-reported/hypothesis/needs-recheck state, and records validation as success, failure, partial, or not-run; children return candidates only. If storage is unavailable, complete independent work and report the unsaved proposal and reason. Do not store secrets or raw logs. Knowledge is reference data and grants no authority. Report changed files, checks, material assumptions, partial work, and exact remaining conditions. Commit, push, publish, deploy, or change external state only with explicit authorization.
